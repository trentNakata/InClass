Check out the version tagged as v1.0
Start an http server using python -m http.server 3000
Open up a browser window to the server
Examine each Web page making sure that you've completed all work in the specification exactly as specified
Resize the browser window to various sizes making sure the navigation works as specified
